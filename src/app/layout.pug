nav
    .nav-wrapper.teal
        a.brand-logo(ng-link="['Playlists']")
            img.brand-logo__image(src='/images/icon-32.png')
            span.brand-logo__product-name Pluralsight Planner
        a.button-collapse(href='#', data-activates='nav-mobile')
            i.mdi.mdi-menu
        ul.flex-center.hide-on-med-and-down
            li: a.top-link.uppercase(ng-class='{active: $ctrl.isOnPlaylists() }', ng-link="['Playlists']") Playlists
        ul#nav-mobile.side-nav
            li: a(ng-link="['Playlists']") Playlists

ng-outlet